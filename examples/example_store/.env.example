# Google Cloud project that owns the Example Store
GOOGLE_CLOUD_PROJECT=my-project

# Example Store is v1beta1 (Preview) and served from us-central1 only
GOOGLE_CLOUD_LOCATION=us-central1

# ID of a pre-provisioned Example Store (the last resource-name segment)
EXAMPLE_STORE_ID=my-example-store
