[package]
name = "quickstart_test"
version = "0.1.0"
edition = "2024"

[dependencies]
# Use local crates instead of published ones
adk-rust = { path = "../../../adk-rust", features = ["openai", "anthropic", "deepseek", "groq", "ollama"] }
tokio = { version = "1.40", features = ["full"] }
dotenvy = "0.15"
